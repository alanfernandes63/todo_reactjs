import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    
    listHeader:{
        fontSize:30,
    },

    divider:{
      marginTop:10
    },

    button:{
      marginTop:10
    }
});

export default useStyles;